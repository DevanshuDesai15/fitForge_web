import { Pressable, StyleSheet, Text, View } from 'react-native';
import { fontSizes, fontWeights, radii, sizes, spacing } from '@/design-system';
export function GoogleButton({ onPress, disabled, loading, label = 'Continue with Google' }: { onPress: () => void; disabled?: boolean; loading?: boolean; label?: string }) { return <Pressable accessibilityRole="button" accessibilityLabel={label} accessibilityState={{ disabled, busy: loading }} disabled={disabled || loading} onPress={onPress} style={({ pressed }) => [styles.button, { opacity: disabled ? 0.42 : pressed ? 0.8 : 1 }]}><View style={styles.mark}><Text style={styles.g}>G</Text></View><Text style={styles.label}>{loading ? 'Opening Google…' : label}</Text></Pressable>; }
const styles = StyleSheet.create({ button: { minHeight: sizes.tapMin, borderRadius: radii.xs, backgroundColor: '#ffffff', paddingHorizontal: spacing[4], flexDirection: 'row', alignItems: 'center', justifyContent: 'center', gap: spacing[3] }, mark: { width: 22, height: 22, borderRadius: 11, backgroundColor: '#f1f3f4', alignItems: 'center', justifyContent: 'center' }, g: { color: '#4285f4', fontFamily: fontWeights.heavy }, label: { color: '#1a1a1a', fontFamily: fontWeights.bold, fontSize: fontSizes.lg } });
